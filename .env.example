# ── Database ──────────────────────────────────────────────────────────────────
# Set DB_DRIVER=mysql for production (Coolify MySQL service).
# Leave as sqlite for local development (no other vars needed).

DB_DRIVER=mysql
DB_HOST=loadmonitoring-loadmonitor-bmuiho
DB_PORT=3306
DB_NAME=load_monitor
DB_USER=load_user
DB_PASS=lo@d@05

# ── App ───────────────────────────────────────────────────────────────────────
APP_BASE_PATH=
